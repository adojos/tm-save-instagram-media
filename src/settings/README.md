# Settings

This component manages versioned application preferences and IndexedDB
persistence. The vault handle and last note destination use separate keys. The
fixed v1 media root is centrally configured and cannot be overridden through
stored settings.
