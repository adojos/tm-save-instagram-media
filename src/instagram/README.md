# Instagram extraction

This component will contain isolated detector, metadata, image, carousel,
and reel extractors. It will emit normalized capture data and must not write
files or invoke storage providers.

Carousel extraction is the first implementation priority.
