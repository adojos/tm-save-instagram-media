# Markdown

This component is a pure generator: normalized capture data and resolved
vault-relative media paths in, Markdown text out. It preserves the original
caption, includes successful primary and auxiliary embeds, validates link
paths, and does not perform filesystem writes.
